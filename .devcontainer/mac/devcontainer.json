{
  "name": "ppg2-ms (Mac, with data_raw)",
  "build": {
    "dockerfile": "../../Dockerfile",
    "context": "../.."
  },
  "workspaceFolder": "/project",
  "workspaceMount": "source=${localWorkspaceFolder},target=/project,type=bind,consistency=cached",
  "mounts": [
    "source=/Users/joelnitta/Library/CloudStorage/Dropbox/project_data/ppg2_ms/data_raw,target=/project/data_raw,type=bind,consistency=cached",
    "source=${localEnv:HOME}/.Renviron,target=/home/rstudio/.Renviron,type=bind,consistency=cached"
  ],
  "remoteUser": "rstudio",
  "updateRemoteUserUID": true,
  "postCreateCommand": "R --no-save -e 'renv::restore()'",
  "customizations": {
    "vscode": {
      "extensions": [
        "REditorSupport.r",
        "REditorSupport.r-debugger",
        "quarto.quarto"
      ],
      "settings": {
        "r.rterm.linux": "/usr/local/bin/R",
        "r.bracketedPaste": true,
        "[r]": {
          "editor.formatOnSave": false
        }
      }
    }
  }
}
